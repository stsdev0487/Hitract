import {align, color, font, layer, viewport, Icon} from 'Hitract/UI';
import ui from './NavigationStyle';
export const Card = {
	cardStyle: ui.cardStyle,
	cardShadowEnabled: true,
	cardOverlayEnabled: false,
	mode: layer.card
}

export const Modal = {mode: layer.modal}